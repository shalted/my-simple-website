# 后续学习计划

更新于 2026-07-28。当前知识库已有 36 篇学习笔记和 17 个交互专题；这份计划只保留仍需学习或继续补强的内容，并用六个章节串起现有知识。

状态说明：

- `已覆盖`：已有文章或交互专题，可以直接复习。
- `继续补强`：已有基础内容，但仍有关键机制没有单独讲清。
- `尚未开始`：知识库还没有对应的系统文章。

## CHAPTER 01 · 基础结构与状态建模

状态：已覆盖，后续以复习和语法补充为主。

已经覆盖：

- List、Dictionary、HashSet、Queue、Stack 的使用边界。
- 集合排序、查找、筛选和常见配套语法。
- Flags / BitMask 的组合、判断、移除和互斥建模。
- 二叉堆、优先队列以及提示队列从 FIFO 到优先堆的演进。

继续补充：

- `struct` 与 `class` 的值语义和引用语义。
- `interface`、泛型方法与职责边界。
- `delegate`、`Action`、`Func`、`event` 的关系。
- 属性、`readonly`、`const`、`static`、nullable 与空值判断。

现有入口：[Flags / BitMask 实验](/knowledge/systems-lab/#flags)、[二叉堆优先队列](/knowledge/binary-heap/)。

## CHAPTER 02 · 算法、空间与碰撞

状态：继续补强。

已经覆盖：

- A* 的 G、H、F、Open / Closed 集合与 Parent 回溯。
- 普通随机、加权随机、稳定随机、保底和洗牌袋。
- 九宫格、空间 Hash、四叉树与“粗筛 → 精筛”。
- Broad Phase / Narrow Phase、AABB、OBB、BVH、SAT、GJK。
- 圆形、扇形、盒形目标筛选与命中原因追踪。

下一批：

1. BFS / DFS：遍历顺序、访问集合和适用问题。
2. Dijkstra：从无启发式最短路理解 A* 的来源。
3. 二分查找、Top K、并查集和动态规划入门。
4. CCD：连续碰撞检测如何处理高速物体穿透。
5. NavMesh 多边形寻路、动态障碍和合法出生点。

现有入口：[A* 寻路](/#lab)、[空间划分实验](/knowledge/systems-lab/#space)、[目标筛选与命中判定](/knowledge/target-filter/)。

## CHAPTER 03 · 游戏 AI、导航与群体行为

状态：继续补强。

已经覆盖：

- 状态机、行为树、效用 AI 与 GOAP 的适用边界。
- RVO / ORCA 的局部避障模型。
- 集群 AI 的槽位分配、候选评分、攻击令牌和非攻击意图。
- 全局寻路、局部移动和群体协调的基本分工。

下一批：

1. NavMesh 烘焙数据与 A* 在多边形图上的搜索过程。
2. 动态障碍、不可达点和出生点合法性检测。
3. 行为树执行栈、节点返回值和中断。
4. HTN 与 MCTS：它们分别适合怎样的决策问题。
5. Steering、RVO、ORCA 的速度空间对比实验。

现有入口：[怪物集群 AI](/knowledge/swarm-ai/)、[状态机、行为树、效用 AI 与 GOAP](/knowledge/library/%E6%B8%B8%E6%88%8FAI%E4%B8%8E%E9%80%BB%E8%BE%91%E5%BB%BA%E6%A8%A1/%E7%8A%B6%E6%80%81%E6%9C%BA%E8%A1%8C%E4%B8%BA%E6%A0%91%E6%95%88%E7%94%A8AI%E4%B8%8EGOAP%E5%AF%B9%E6%AF%94/)。

## CHAPTER 04 · 游戏运行时与战斗系统

状态：继续补强；完整技能运行链已经形成。

已经覆盖：

- 冷却、计时器、延迟、Tick 与时间缩放。
- 对象池、资源生命周期、资源作用域和引用释放。
- 技能激活、标签仲裁、Timeline 编排、目标筛选、Effect / Buff、反馈与清理。
- 投射物、击退曲线、碰撞提前停止和终态清理。
- Dirty Flag、属性局部重算和失效引用清理。

下一批：

1. 对象生命周期状态：`Created → Active → Releasing → Released`。
2. 异步回调晚于对象释放时，如何保留原始失败并阻止越界写入。
3. 缓存建立、版本、失效、重建和不同作用域。
4. 命令模式：排队、撤销，以及命令与事件的区别。
5. 事件系统进阶：事件队列、延迟事件、一次性监听、调试和事件风暴。
6. 完整战斗判定：攻击盒、受击盒、防御、无敌、霸体、硬直、反击与反馈。

现有入口：[一次技能从输入到结束](/knowledge/ability-flow/)、[Effect / Buff 生命周期](/knowledge/effect-lifecycle/)、[命中后的运动与投射物](/knowledge/projectile-motion/)。

## CHAPTER 05 · UI、配置与网络闭环

状态：本阶段已覆盖，后续以专项深化为主。

已经覆盖：

- 虚拟列表的可视窗口、节点复用、索引映射与异步取消。
- UI 页面栈、面板池、覆盖与返回、绑定清理和旧回调隔离。
- 配置数据的结构校验、引用校验、语义校验、强类型快照与发布闸门。
- 网络预测、服务器权威状态、Reconcile 和输入重放。

可选深化：

- UI 数据绑定的依赖追踪与批量刷新。
- 配置热更新的版本一致性和失败可见性。
- 网络预测中的抖动缓冲、快照插值与可观测性。

现有入口：[虚拟列表与节点复用](/knowledge/virtual-list/)、[配置数据生成管线](/knowledge/config-pipeline/)、[网络预测与服务器校正](/knowledge/network-prediction/)、[UI 页面栈与面板池](/knowledge/ui-stack-pool/)。

## CHAPTER 06 · 工程架构与底层能力

状态：尚未开始，是后续长期主线。

下一批：

1. UE Gameplay：GameMode、GameState、PlayerController、Pawn、Actor 与 Component。
2. ECS：Entity、Component、System 和数据布局。
3. 对话系统：节点、选项、条件、效果与跳转。
4. C++ 对象模型：虚表、虚指针和动态绑定。
5. RAII、构造析构、资源所有权与智能指针。
6. 左值、右值、移动语义、线程、锁、任务队列和生产者消费者。
7. 虚拟内存、页、堆、栈以及 malloc / free。
8. 原子操作、可见性、有序性、内存屏障、进程与上下文切换。

这些主题先写成结构清晰的文章；只有存在可观察状态、队列、图结构或计算过程时，再升级为交互专题。

## 推荐推进顺序

下一轮从第一个未完成主题开始：

1. 对象生命周期状态与异步竞态。
2. 缓存与失效策略。
3. 命令模式与事件系统进阶。
4. 完整战斗判定流程。
5. NavMesh、动态障碍与合法点检测。
6. CCD 高速碰撞。
7. BFS / DFS、Dijkstra 与算法补充线。
8. HTN / MCTS 与行为树执行细节。
9. UE Gameplay、ECS 与对话系统。
10. C++、内存与并发基础。

每完成一个主题，都要同步更新这份计划、网站章节目录、知识索引和相关交叉链接。
