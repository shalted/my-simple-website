# 狮团团中秋版本：并行任务与整合边界

## 用户已确认（2026-09-17）

- 保留现有全部角色美术素材，不重新设计角色，仅改名称与正式文案。
- 沿用本轮接受的命名提案：《狮团团·月饼夺回战》；霜尾、追风、桂团、沧澜、赤轮、曜瞳；护饼卫、运饼小妖、掷饼小妖、年兽。
- 内部角色 ID 与素材键保留，玩家可见名称与背景改为狮团团伙伴故事。
- 新增主界面、图鉴、首回合操作引导、真实进度加载界面。
- 漫画可发挥创意，但必须保留角色外形；开场追饼、胜利后全员分享月饼祝中秋快乐，年兽与三个小怪不可缺席。
- 使用静态漫画逐格演出，做好动态效果、跳过与重播。
- 允许新开并行任务；不代表允许任意改经济、猜测固定值或引入错误回退。

## 当前基线

- 实际仓库：E:/Simple/my-simple-website；保存项目 E:/Simple 不是 Git 根目录。
- 已有本地提交 229a5ce，上一轮推送失败；敌方接入等另有未提交修改，必须保留。
- 四套敌人已本地接入，64 项测试与首战/最终波浏览器验收通过。
- 当前 assets.js 为 50,139,891 字节，尚有分包与发布阻塞。

## 文件所有权

| 任务 | ID | 可修改范围 |
| --- | --- | --- |
| 角色文案与难度 | 01a0ad07-9146-7a73-bb75-1800c50e7369 | characters.js、enemies.js、synergies.js、skills.js、equipment.js、campaign.js；独立文案测试与交接 |
| 主界面与团圆图鉴 | 01a0ad07-a58f-7f10-9606-7f3fc290e425 | 新 lobby.js、lobby.css；本任务独立预览、测试、交接 |
| 剧情漫画与动态演出 | 01a0ad07-beb6-79e0-b59d-e056258f7411 | 新 story/ 素材、story-player.js、story.css；本任务独立预览、测试、交接 |
| 资源加载与首回合引导 | 01a0ad07-d048-7e33-a867-951f24797cfb | 新 resource-loader.js、tutorial.js、onboarding.css、资源构建脚本及 resources/；独立预览、测试、交接 |
| 总控 | 01a0a7d6-14a4-7011-87eb-423964d12f5f | index.html、styles.css、ui.js、engine.js、renderer.js、package.json、现有主测试、旧素材构建/部署配置的最终接线 |

禁止各任务为了独立预览改写正式入口，禁止同时创建多份正式 Campaign。新模块通过调用方传入资源、配置、事件和回调；具体接口由产出任务在交接文档说明，缺口向总控报告。

## 依赖和验收顺序

1. 文案配置先统一新名称，其他界面读取配置，不各自复制一套命名表。
2. 主界面、漫画播放器、加载/引导先各自独立预览；不冒充正式全流程接入完成。
3. 新资源方案确认后，总控切换正式加载入口并排除旧大包，不通过扩大限额或静默替换素材绕过错误。
4. 总控串联加载 → 主界面/图鉴 → 开场漫画 → 引导与战斗 → 成败结算 → 胜利漫画/返回主界面。
5. 逐项验证首次进入、跳过、重播、重玩、战败、最终波胜利、加载失败和移动端；保留回归中的真实错误。
6. 代码提交/上线单独验收，模块完成不等于已经发布。

## 尚需具体确认的实施参数

- 敌方成长和 Boss 强化的具体数值。
- 引导所需固定商品、金币、出售教学的经济处理。
- 最终波失败后的结束/重试规则，不能继续生成第十一回合或伪造通关。
- 新资源路径/存储键/重试与降级等需要确认的设计，在实施任务集中提出；不得遗漏原始错误。

以上是协作计划而非已实现状态。每个任务完成后需提供精确文件、接口、实际测试与剩余问题，总控再核验。

## 总控接线检查点（2026-09-17）

- 角色/技能/装备/羁绊正式文案已接入共享配置；index 和战斗档案、结算标题也已同步。旧内部 ID 与角色美术保持不变。
- 正式入口已从 assets.js 切换为 resource-loader.js + resources/manifest.json。40 项资源逐字节保留，按实际收到字节显示进度并校验 SHA-256；下载/解码错误直接显示，无重试、替图或旧包回退。
- `.assetsignore` 已单独排除旧 assets.js；构建命令串联旧素材打包和外置资源生成。旧文件保留为构建来源，不删除原图。
- 主回归已纳入内容和资源/教程检查；最近运行 72/72 通过，之后教程任务新增一项真实 Campaign 可达性测试，最终合并需重新运行。
- 正式浏览器验证：内置浏览器看到真实进度后进入棋盘；Chrome 显示新角色和羁绊名称，购买经验后人口 3→4，战斗正常开启，控制台 warning/error 为 0。Chrome 后续显示第 2 回合准备状态；未将此记为完整十回合玩家验收。
- 内置浏览器仍显示部分旧羁绊缓存，磁盘/HTTP 响应和 Chrome 均已是新名称；尚未增加任何缓存回退或版本替代规则。
- 大厅/图鉴独立模块已交付并视觉复验，尚未接到正式入口。漫画八张图与独立播放器已产出，正在收紧参数校验与销毁清理；总控已目检终幕十位同框。
- 正式教程暂不接入：已向用户确认“购买备战伙伴→交换上阵→移动→下阵或出售→经验升级→开战”的可达顺序，不改经济。
- 难度曲线、最终波失败分支仍待用户确认；未修改数值，也未把不完整胜负规则接成胜利漫画。
- 尚未提交或发布。本检查点只说明本地已验证范围，不代表大厅、漫画、引导端到端集成完毕。
